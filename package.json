{
  "name": "koa-typescript-api",
  "version": "1.0.0",
  "description": "api 接口",
  "main": "server.js",
  "scripts": {
    "remove": "rm -rf dist",
    "watch": "yarn run remove && yarn run tslint && tsc -w",
    "build": "yarn run remove && yarn run tslint && tsc",
    "dev": "./node_modules/.bin/nodemon dist/server.js",
    "start": "pm2 start dist/server.js --name koa-typescript-api",
    "tslint": "tslint -c tslint.json -p tsconfig.json"
  },
  "repository": {
    "type": "git",
    "url": "https://gitee.com/cuilanchao/koa-typescript-api.git"
  },
  "author": "晚秋",
  "license": "ISC",
  "dependencies": {
    "@types/reflect-metadata": "^0.1.0",
    "koa": "^2.13.1",
    "koa-controllers": "^0.1.6",
    "koa-router": "^10.0.0",
    "log4js": "^6.3.0",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@types/koa": "^2.13.4",
    "@types/koa-router": "^7.4.4",
    "nodemon": "^2.0.12",
    "ts-node": "^10.1.0",
    "tslint": "^6.1.3",
    "typescript": "^4.3.5"
  }
}
